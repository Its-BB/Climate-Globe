// Placeholder shell. Segment 09 replaces this with the real application once
// the globe, data, and UI pieces exist. For now it just proves the styled
// canvas renders.
export default function App() {
  return (
    <div className="app">
      <div className="title-bar">
        <h1>Climate Globe</h1>
      </div>
    </div>
  );
}
